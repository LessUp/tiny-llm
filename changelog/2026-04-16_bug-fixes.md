---
layout: default
title: "v2.0.1 — Bug 修复 (2026-04-16)"
description: "QuantizedWeight scale 尺寸修复、Attention 内核代码清理"
---

# v2.0.1 — Bug 修复

**日期**: 2026-04-16  
**类型**: Bug Fix

---

## 修复

### QuantizedWeight scale 尺寸计算错误

**严重**: Critical  
**文件**: `tests/test_integration.cu`

**问题**: `createRandomWeight` 计算 scales 尺寸错误。

```cpp
// 错误
int num_groups = (cols + group_size - 1) / group_size;
w.scales = randomDeviceFP16(rows * num_groups, ...);

// 正确
int num_groups = (rows + group_size - 1) / group_size;
w.scales = randomDeviceFP16(num_groups * cols, ...);
```

**影响**: W8A16 matmul 按 `[k/group_size, col]` 索引 scales，需要 `ceil(rows/g) * cols` 个元素。

---

### Attention 内核冗余代码

**文件**: `kernels/attention.cu`

**问题**: `attention_decode_kernel` 加载 `q_reg` 数组但未使用。

**修复**: 移除 12 行未使用代码。

---

## 变更摘要

| 文件 | 类型 |
|------|------|
| `tests/test_integration.cu` | Bug Fix |
| `kernels/attention.cu` | Cleanup |

---

[← 返回](index)
