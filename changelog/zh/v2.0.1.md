---
layout: default
title: "v2.0.1 — Bug 修复"
description: "QuantizedWeight 尺度维度修复和代码清理"
nav_order: 1
---

# v2.0.1 — Bug 修复

**发布日期**: 2026年4月16日  
**类型**: Bug 修复版本

---

## 修复内容

### QuantizedWeight 尺度维度计算错误

**严重程度**: Critical  
**文件**: `tests/test_integration.cu`

`createRandomWeight` 函数的尺度张量维度计算错误。

```cpp
// 错误
int num_groups = (cols + group_size - 1) / group_size;
w.scales = randomDeviceFP16(rows * num_groups, ...);

// 正确
int num_groups = (rows + group_size - 1) / group_size;
w.scales = randomDeviceFP16(num_groups * cols, ...);
```

**影响**: W8A16 矩阵乘使用 `[rows/group_size, cols]` 索引尺度，需要 `ceil(rows/g) * cols` 个元素。

### 注意力 Kernel 清理

**文件**: `kernels/attention.cu`

移除了 `attention_decode_kernel` 中 12 行未使用的 `q_reg` 数组加载代码。

---

## 验证

- ✅ 所有单元测试通过
- ✅ 集成测试通过
- ✅ 格式检查通过

---

[← 返回更新日志](../)
